meta {
  name: 05 - set next request jump
  type: http
  seq: 5
}

get {
  url: https://httpbin.org/get?stage=05
  body: none
  auth: none
}

tests {
  test("05 - status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  bru.setNextRequest("07 - jump target");
}
